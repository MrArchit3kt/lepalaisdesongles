/*
 * Le routeur UploadThing possède une seule source de vérité.
 *
 * Ce fichier reste présent pour la compatibilité avec les imports
 * conventionnels UploadThing, mais ne définit plus un second routeur.
 */

export {
  uploadRouter,
} from "@/server/uploadthing";

export type {
  UploadRouter,
} from "@/server/uploadthing";
