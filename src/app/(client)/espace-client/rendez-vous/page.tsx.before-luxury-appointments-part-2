import Link from "next/link";
import {
  CalendarCheck2,
  CalendarDays,
  ChevronRight,
  Clock3,
  CreditCard,
  History,
  XCircle,
} from "lucide-react";

import { prisma } from "@/lib/prisma";
import { requireClientUser } from "@/lib/session";

export const dynamic = "force-dynamic";

type SearchParams = Promise<{
  filtre?: string;
}>;

type AppointmentStatusFilter =
  | "a-venir"
  | "termines"
  | "annules"
  | "expires";

type AppointmentsPageProps = {
  searchParams: SearchParams;
};

function formatCurrency(cents: number): string {
  return new Intl.NumberFormat("fr-FR", {
    style: "currency",
    currency: "EUR",
  }).format(cents / 100);
}

function formatDate(value: Date): string {
  return new Intl.DateTimeFormat("fr-FR", {
    timeZone: "Europe/Paris",
    weekday: "long",
    day: "numeric",
    month: "long",
    year: "numeric",
  }).format(value);
}

function formatTime(value: Date): string {
  return new Intl.DateTimeFormat("fr-FR", {
    timeZone: "Europe/Paris",
    hour: "2-digit",
    minute: "2-digit",
  }).format(value);
}

function normalizeFilter(
  value: string | undefined,
): AppointmentStatusFilter {
  if (
    value === "termines" ||
    value === "annules" ||
    value === "expires"
  ) {
    return value;
  }

  return "a-venir";
}

function getStatusLabel(
  status: string,
  paymentStatus: string,
): {
  label: string;
  className: string;
} {
  if (status === "CONFIRMED") {
    return {
      label: "Confirmé",
      className:
        "border border-emerald-200 bg-emerald-50 text-emerald-700 shadow-sm",
    };
  }

  if (status === "PENDING") {
    return {
      label:
        paymentStatus === "PAID"
          ? "Paiement reçu"
          : "Paiement en attente",
      className:
        "border border-[#E8D39F] bg-[#FFF9E9] text-[#9A6A18] shadow-sm",
    };
  }

  if (status === "COMPLETED") {
    return {
      label: "Terminé",
      className:
        "border border-[#DABCCA] bg-[#FFF1F6] text-[#843F59] shadow-sm",
    };
  }

  if (
    status === "CANCELLED_BY_CLIENT" ||
    status === "CANCELLED_BY_ADMIN"
  ) {
    return {
      label: "Annulé",
      className:
        "border border-red-200 bg-red-50 text-red-700 shadow-sm",
    };
  }

  if (status === "EXPIRED") {
    return {
      label: "Expiré",
      className:
        "border border-[#E6D8DD] bg-[#F8F4F5] text-[#806C74]",
    };
  }

  if (status === "REFUSED") {
    return {
      label: "Refusé",
      className:
        "border border-red-200 bg-red-50 text-red-700 shadow-sm",
    };
  }

  return {
    label: status,
    className:
      "border border-[#E6D8DD] bg-[#F8F4F5] text-[#806C74]",
  };
}

export default async function AppointmentsPage({
  searchParams,
}: AppointmentsPageProps) {
  const user = await requireClientUser();
  const { filtre } = await searchParams;
  const activeFilter = normalizeFilter(filtre);
  const now = new Date();

  const appointments =
    await prisma.appointment.findMany({
      where:
        activeFilter === "a-venir"
          ? {
              clientId: user.id,
              startsAt: {
                gte: now,
              },
              status: {
                in: ["PENDING", "CONFIRMED"],
              },
            }
          : activeFilter === "termines"
            ? {
                clientId: user.id,
                status: "COMPLETED",
              }
            : activeFilter === "annules"
              ? {
                  clientId: user.id,
                  status: {
                    in: [
                      "CANCELLED_BY_CLIENT",
                      "CANCELLED_BY_ADMIN",
                      "REFUSED",
                    ],
                  },
                }
              : {
                  clientId: user.id,
                  status: "EXPIRED",
                },

      orderBy:
        activeFilter === "a-venir"
          ? {
              startsAt: "asc",
            }
          : {
              startsAt: "desc",
            },

      select: {
        id: true,
        reference: true,
        status: true,
        paymentStatus: true,
        startsAt: true,
        endsAt: true,
        totalPriceCents: true,
        depositCents: true,
        cancellationReason: true,

        staff: {
          select: {
            displayName: true,

            user: {
              select: {
                firstName: true,
                lastName: true,
              },
            },
          },
        },

        services: {
          orderBy: {
            sortOrder: "asc",
          },

          select: {
            id: true,
            serviceName: true,
            quantity: true,
          },
        },

        _count: {
          select: {
            images: true,
          },
        },

        review: {
          select: {
            id: true,
            rating: true,
            status: true,
          },
        },
      },
    });

  const tabs = [
    {
      key: "a-venir",
      label: "À venir",
      icon: CalendarDays,
    },
    {
      key: "termines",
      label: "Terminés",
      icon: CalendarCheck2,
    },
    {
      key: "annules",
      label: "Annulés",
      icon: XCircle,
    },
    {
      key: "expires",
      label: "Expirés",
      icon: History,
    },
  ] satisfies Array<{
    key: AppointmentStatusFilter;
    label: string;
    icon: typeof CalendarDays;
  }>;

  return (
    <main className="relative min-h-screen overflow-hidden bg-[radial-gradient(circle_at_top_right,rgba(232,180,192,0.20),transparent_30%),radial-gradient(circle_at_bottom_left,rgba(214,182,121,0.10),transparent_32%),linear-gradient(135deg,#FFF8FA_0%,#FFFDFC_48%,#FFF4F7_100%)] px-4 py-8 sm:px-6 lg:py-12">
      <div className="relative z-10 mx-auto max-w-6xl">
        <div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between">
          <div>
            <Link
              href="/espace-client"
              className="inline-flex items-center rounded-full border border-[#E8C3CF] bg-white/80 px-4 py-2 text-sm font-black text-[#A5526D] shadow-sm transition hover:border-[#D8AAB9] hover:bg-[#FFF0F4] hover:text-[#843F59]"
            >
              ← Retour à mon espace
            </Link>

            <p className="mt-6 text-[10px] font-black uppercase tracking-[0.22em] text-[#A5526D]">
              Mon espace
            </p>

            <h1 className="mt-2 font-serif text-4xl font-semibold tracking-tight text-[#2F2027] sm:text-5xl">
              Mes rendez-vous
            </h1>

            <p className="mt-4 max-w-2xl leading-7 text-[#816D75]">
              Consultez vos rendez-vous à venir,
              terminés, annulés ou expirés.
            </p>
          </div>

          <Link
            href="/reservation"
            className="inline-flex items-center justify-center rounded-2xl bg-gradient-to-r from-[#B45F7A] to-[#843F59] px-5 py-3 text-sm font-black text-white shadow-[0_12px_28px_rgba(132,63,89,0.22)] transition hover:-translate-y-0.5 hover:shadow-[0_16px_34px_rgba(132,63,89,0.28)]"
          >
            Réserver un rendez-vous
          </Link>
        </div>

        <nav className="mt-9 grid gap-3 rounded-[1.75rem] border border-[#EFDEE4] bg-white/95 p-3 shadow-[0_16px_40px_rgba(85,38,55,0.07)] backdrop-blur sm:grid-cols-4">
          {tabs.map((tab) => {
            const Icon = tab.icon;
            const isActive =
              activeFilter === tab.key;

            return (
              <Link
                key={tab.key}
                href={`/espace-client/rendez-vous?filtre=${tab.key}`}
                className={
                  isActive
                    ? "flex items-center justify-center gap-2 rounded-2xl bg-gradient-to-r from-[#B45F7A] to-[#843F59] px-4 py-3 text-sm font-black text-white shadow-[0_9px_22px_rgba(132,63,89,0.22)]"
                    : "flex items-center justify-center gap-2 rounded-2xl border border-transparent px-4 py-3 text-sm font-semibold text-[#705D65] transition hover:border-[#E8C3CF] hover:bg-[#FFF3F6] hover:text-[#843F59]"
                }
              >
                <Icon className="size-4" />
                {tab.label}
              </Link>
            );
          })}
        </nav>

        <section className="mt-8 space-y-5">
          {appointments.length === 0 ? (
            <div className="rounded-[2rem] border border-dashed border-[#D9B4C0] bg-gradient-to-br from-white to-[#FFF4F7] p-10 text-center shadow-[0_18px_45px_rgba(85,38,55,0.06)]">
              <span className="mx-auto flex size-16 items-center justify-center rounded-2xl border border-[#E8C3CF] bg-gradient-to-br from-[#FFF0F5] to-[#F4D4DE] text-[#A5526D] shadow-sm">
                <CalendarDays className="size-6" />
              </span>

              <h2 className="mt-5 font-serif text-2xl font-semibold text-[#2F2027]">
                Aucun rendez-vous
              </h2>

              <p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#816D75]">
                Aucun rendez-vous ne correspond à
                cette catégorie.
              </p>

              {activeFilter === "a-venir" ? (
                <Link
                  href="/reservation"
                  className="mt-6 inline-flex rounded-2xl bg-gradient-to-r from-[#B45F7A] to-[#843F59] px-6 py-3 text-sm font-black text-white shadow-[0_12px_28px_rgba(132,63,89,0.22)] transition hover:-translate-y-0.5"
                >
                  Réserver maintenant
                </Link>
              ) : null}
            </div>
          ) : (
            appointments.map((appointment) => {
              const status = getStatusLabel(
                appointment.status,
                appointment.paymentStatus,
              );

              const staffName =
                appointment.staff?.displayName?.trim() ||
                [
                  appointment.staff?.user.firstName,
                  appointment.staff?.user.lastName,
                ]
                  .filter(Boolean)
                  .join(" ") ||
                "Le Palais des Ongles";

              const serviceNames =
                appointment.services
                  .map((service) =>
                    service.quantity > 1
                      ? `${service.serviceName} × ${service.quantity}`
                      : service.serviceName,
                  )
                  .join(", ");

              const paidCents =
                appointment.paymentStatus ===
                "PAID"
                  ? appointment.depositCents
                  : 0;

              const remainingCents = Math.max(
                appointment.totalPriceCents -
                  paidCents,
                0,
              );

              return (
                <article
                  key={appointment.id}
                  className="group overflow-hidden rounded-[2rem] border border-[#EFDEE4] bg-white/95 shadow-[0_18px_48px_rgba(85,38,55,0.08)] transition duration-300 hover:-translate-y-0.5 hover:border-[#DDBAC5] hover:shadow-[0_24px_58px_rgba(132,63,89,0.12)]"
                >
                  <div className="grid gap-0 lg:grid-cols-[1fr_280px]">
                    <div className="p-6 sm:p-8">
                      <div className="flex flex-wrap items-center justify-between gap-3">
                        <span
                          className={`rounded-full px-3 py-1.5 text-xs font-black ${status.className}`}
                        >
                          {status.label}
                        </span>

                        <span className="rounded-full bg-[#FBF6F8] px-3 py-1.5 text-xs font-semibold text-[#816D75]">
                          Référence{" "}
                          {appointment.reference}
                        </span>
                      </div>

                      <h2 className="mt-6 font-serif text-2xl font-semibold text-[#2F2027] sm:text-3xl">
                        {serviceNames ||
                          "Rendez-vous au salon"}
                      </h2>

                      <div className="mt-5 grid gap-4 sm:grid-cols-3">
                        <div className="flex gap-3 rounded-[1.25rem] border border-[#EFDEE4] bg-gradient-to-br from-white to-[#FFF5F8] p-4 shadow-sm">
                          <CalendarDays className="mt-0.5 size-5 shrink-0 text-[#A5526D]" />

                          <div>
                            <p className="text-[10px] font-black uppercase tracking-[0.14em] text-[#A68C96]">
                              Date
                            </p>

                            <p className="mt-1 text-sm font-semibold capitalize text-[#241A1D]">
                              {formatDate(
                                appointment.startsAt,
                              )}
                            </p>
                          </div>
                        </div>

                        <div className="flex gap-3 rounded-[1.25rem] border border-[#EFDEE4] bg-gradient-to-br from-white to-[#FFF5F8] p-4 shadow-sm">
                          <Clock3 className="mt-0.5 size-5 shrink-0 text-[#A5526D]" />

                          <div>
                            <p className="text-[10px] font-black uppercase tracking-[0.14em] text-[#A68C96]">
                              Horaire
                            </p>

                            <p className="mt-1 text-sm font-semibold text-[#241A1D]">
                              {formatTime(
                                appointment.startsAt,
                              )}
                              {" – "}
                              {formatTime(
                                appointment.endsAt,
                              )}
                            </p>
                          </div>
                        </div>

                        <div className="flex gap-3 rounded-[1.25rem] border border-[#EFDEE4] bg-gradient-to-br from-white to-[#FFF5F8] p-4 shadow-sm">
                          <CreditCard className="mt-0.5 size-5 shrink-0 text-[#A5526D]" />

                          <div>
                            <p className="text-[10px] font-black uppercase tracking-[0.14em] text-[#A68C96]">
                              Paiement
                            </p>

                            <p className="mt-1 text-sm font-semibold text-[#241A1D]">
                              {appointment.paymentStatus ===
                              "PAID"
                                ? `${formatCurrency(
                                    paidCents,
                                  )} versés`
                                : "En attente"}
                            </p>
                          </div>
                        </div>
                      </div>

                      <div className="mt-5 flex flex-wrap gap-x-6 gap-y-2 text-sm text-[#75636A]">
                        <span>
                          Avec{" "}
                          <strong className="text-[#241A1D]">
                            {staffName}
                          </strong>
                        </span>

                        {appointment._count.images >
                        0 ? (
                          <span>
                            {
                              appointment._count
                                .images
                            }{" "}
                            photo
                            {appointment._count
                              .images > 1
                              ? "s"
                              : ""}
                          </span>
                        ) : null}

                        {appointment.review ? (
                          <span>
                            Avis :{" "}
                            {appointment.review.rating}
                            /5
                          </span>
                        ) : null}
                      </div>

                      {appointment.cancellationReason ? (
                        <p className="mt-5 rounded-2xl bg-red-50 p-4 text-sm leading-6 text-red-700">
                          {
                            appointment.cancellationReason
                          }
                        </p>
                      ) : null}
                    </div>

                    <div className="border-t border-[#241A1D]/8 bg-[#FFF5F3] p-6 lg:border-l lg:border-t-0">
                      <div className="space-y-3 text-sm">
                        <div className="flex justify-between gap-4 text-[#75636A]">
                          <span>Total</span>

                          <strong className="text-[#241A1D]">
                            {formatCurrency(
                              appointment.totalPriceCents,
                            )}
                          </strong>
                        </div>

                        <div className="flex justify-between gap-4 text-[#75636A]">
                          <span>Versé</span>

                          <strong className="text-[#241A1D]">
                            {formatCurrency(
                              paidCents,
                            )}
                          </strong>
                        </div>

                        <div className="flex justify-between gap-4 border-t border-[#241A1D]/10 pt-3">
                          <span className="font-semibold text-[#241A1D]">
                            Solde
                          </span>

                          <strong className="text-lg text-[#9D6F80]">
                            {formatCurrency(
                              remainingCents,
                            )}
                          </strong>
                        </div>
                      </div>

                      <Link
                        href={`/espace-client/rendez-vous/${appointment.reference}`}
                        className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-[#241A1D] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#3A2A2F]"
                      >
                        Voir le détail
                        <ChevronRight className="size-4" />
                      </Link>
                    </div>
                  </div>
                </article>
              );
            })
          )}
        </section>
      </div>
    </main>
  );
}
